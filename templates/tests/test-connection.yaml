apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "handbrake-watcher.fullname" . }}-test-connection"
  labels:
    {{- include "handbrake-watcher.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "handbrake-watcher.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
